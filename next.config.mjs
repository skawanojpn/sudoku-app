// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // 例: 画像最適化のドメイン設定 (もし外部ドメインから画像を読み込む場合)
  // images: {
  //   domains: ['example.com'],
  // },
  // その他のNext.js設定
  // experimental: {
  //   serverActions: true, // サーバーアクションを有効にする場合
  // },
};

export default nextConfig;